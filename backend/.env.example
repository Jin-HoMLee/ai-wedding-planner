# Example environment variables for AI Wedding Planner backend
# Copy this file to .env and fill in your actual credentials/secrets.
# DO NOT commit your .env file to version control.
# Backend Environment Variables

# Port for backend Express server
PORT=

# MongoDB connection string
# For local development, use:
# MONGODB_URI=mongodb://localhost:27017/ai-wedding-planner

# For MongoDB Atlas (cloud), use a string like:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGODB_URI=

# Environment (development or production)
NODE_ENV=development  # Change to 'production' in production environment

# NOTE: Copy this file to .env and fill in your actual credentials/secrets.
# DO NOT commit your .env file to version control.